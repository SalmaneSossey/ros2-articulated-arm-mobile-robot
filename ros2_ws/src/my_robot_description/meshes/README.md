# Meshes Directory

This TP uses primitive URDF and Xacro geometry only, so no custom mesh assets are required.

The directory is kept in the package structure for future extensions and to match common ROS description package layout.
